# docker compose 宿主侧变量（复制为 .env 后按机器改）
# DATA_ROOT: 你的数据集所在目录（挂载为容器内 /data，注册路径 jail 到这里）
DATA_ROOT=/data/datasets
# WORKSPACE_ROOT: 索引/图表资产/secrets 所在目录（需可写；建议 ext4/SSD）
WORKSPACE_ROOT=~/data-agent-workspace
