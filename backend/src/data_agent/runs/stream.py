"""UIMessage stream assembly via VercelAIAdapter granular methods (M2).

Contract to freeze in M2: build_run_input / run_stream / encode_stream, plus the
domain-event mapping table in design-architecture §2.4 / §3.2. Spike A verifies
the adapter surface on the installed pydantic-ai version.
"""
